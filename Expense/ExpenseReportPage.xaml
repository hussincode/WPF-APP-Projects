<Page x:Class="Expense.ExpenseReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Expense"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ExpenseReportPage">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="E:\oop study\Expense\Images\astronaut-black-hole-space-8k-wallpaper-uhdpaper.com-537@5@f.jpg"/>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="239"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Column="1" 
               VerticalAlignment="Center"
               FontWeight="Bold"
               FontSize="20"
               Foreground="Blue"
               Content="Expance Report for:"/>

        <Grid Margin="10"
              Grid.Column="1"
              Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>

            </Grid.RowDefinitions>

            <StackPanel Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Grid.Row="0"
                        Orientation="Horizontal"
                        >
                <Label Margin="0,0,0,5 "
                       FontWeight="Bold"
                       Foreground="White"
                       Content="Name:"/>
                
                <Label Margin="0,0,0,5 "
                       FontWeight="Bold"/>
            </StackPanel>

            <StackPanel Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Grid.Row="1"
                        Orientation="Horizontal">
                <Label Margin="0,0,0,5"
                       FontWeight="Bold"
                       Foreground="White"
                       Content="Department:"/>

                <Label Margin="0,0,0,5"
                       FontWeight="Bold"/>

            </StackPanel>

            <Grid Grid.Column="0"
                  Grid.ColumnSpan="2"
                  Grid.Row="2"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Left">
                <DataGrid AutoGenerateColumns="False"
                          RowHeaderWidth="0">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Background" Value="BlueViolet"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Expensetype"/>
                        <DataGridTextColumn Header="Amount:"/>
                    </DataGrid.Columns>
                </DataGrid>
                          
            </Grid>
                        

        </Grid>

    </Grid>
</Page>
